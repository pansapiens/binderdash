{
    "name": "binderdash",
    "version": "0.1.0",
    "description": "Single-page app to view results of de novo protein binder design runs",
    "private": true,
    "scripts": {
        "test": "node run-tests.js",
        "test:playwright": "pnpm exec playwright test",
        "test:ui": "pnpm exec playwright test --ui",
        "test:headed": "pnpm exec playwright test --headed",
        "test:debug": "pnpm exec playwright test --debug"
    },
    "devDependencies": {
        "@playwright/test": "^1.55.0"
    }
}