# Optional overrides when running the desktop launcher in development.
# BINDERDASH_DESKTOP and DISABLE_AUTHENTICATION are always set by desktop/main.py.

# LOG_LEVEL=DEBUG
# RUN_BASE_DIRS="/data/runs"
# BINDERDASH_DESKTOP_CONFIG="/tmp/binderdash-desktop.json"
