[project]
name = "binderdash-desktop"
version = "0.3.0"
description = "Desktop packaging dependencies for Binderdash"
requires-python = ">=3.11"
dependencies = [
    "pywebview>=5",
    "pyinstaller>=6",
]
